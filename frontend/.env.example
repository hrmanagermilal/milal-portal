# Optional: set only when frontend must call a different origin directly.
# Default is same-origin ("").
VITE_API_BASE_URL=

# Optional for Vite dev server proxy (/api -> target)
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
